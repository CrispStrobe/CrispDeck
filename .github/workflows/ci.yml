name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
          cache: npm

      - run: npm ci
      - run: npm test
      - run: npx svelte-kit sync
      - run: npx vite build

  e2e:
    name: Playwright E2E
    runs-on: ubuntu-latest
    needs: frontend
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
          cache: npm

      - run: npm ci
      - run: npx svelte-kit sync && npx vite build
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test --reporter=list

  rust-check:
    name: Rust check (${{ matrix.platform }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-24.04
          - platform: macos-14
          - platform: windows-latest
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev

      # `cargo check` type-checks and stops. It never builds the test
      # binaries, so the unit tests in src-tauri had never run in CI — they
      # could have been broken for months without anyone finding out. The
      # Rust side holds credential encryption, the Mastodon OAuth exchange
      # and the whole local database, so it is the last place that should be
      # unverified.
      - name: cargo test
        working-directory: src-tauri
        run: cargo test --lib --locked

      # Kept separately: `cargo test` builds the lib and its tests, but not
      # the binary target, and a break there would still ship.
      - name: cargo check
        working-directory: src-tauri
        run: cargo check --locked
